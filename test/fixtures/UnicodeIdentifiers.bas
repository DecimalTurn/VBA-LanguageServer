Attribute VB_Name = "UnicodeIdentifiers"
Option Explicit

' Test module for Unicode identifiers with Latin accented characters
Public Sub TestUnicodeIdentifiers()
    ' Variables with Latin accented characters
    Dim café As String
    Dim naïve As String
    Dim résumé As String
    Dim piñata As String
    Dim señor As String
    Dim mañana As String
    Dim jalapeño As String
    Dim façade As String
    Dim björk As String
    Dim José As String
    Dim François As String
    Dim Müller As String
    Dim Łukasz As String
    'TODO: Add support for greek and cyrillic characters
    'Dim Αθήνα As String
    'Dim москва As String
    Dim messäge As String
    Dim calculér As String
    Dim numbér As String
End Sub
